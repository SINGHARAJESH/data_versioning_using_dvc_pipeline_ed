from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='a project where we use dvc pipeline with data versoing',
    author='Rajesh Singha',
    license='',
)
